import React from 'react' 

const PictureContext = React.createContext(null)

// const PictureProvider = PictureContext.Provider
// const PictureConsumer = PictureContext.Consumer

// export { PictureProvider, PictureConsumer}
export default PictureContext